# 第三轮优化总结（2026-08-11）

> 本轮目标：**围绕「前端性能优化 · 稳定性治理 · 用户体验 · 工程化落地」四大方向，攻克前两轮遗留的高杠杆缺口，并把可自证的能力写进简历。**
> 范围（与用户确认）：精选高性价比（约 10 项）+ 后端纳入改造（内存缓存 + 访问日志）+ 产出全部新建独立文件。
> 性质：前端 + 后端全链路改造（前两轮均为纯前端或纯 bug 修复）。

---

## 一、本轮优化清单（10 项，按四大方向分类）

### 方向一：前端性能优化

| # | 位置 | 问题 | 修复 | 量化 |
|---|---|---|---|---|
| E10 | [App.vue](frontend/src/App.vue#L59-L71) | 播放状态保存用一个 `{ deep: true }` watch 监听整个 player store：播放中 `currentTime` 每秒约 4 次更新，每次触发**深遍历整个 playlist 对象树** | 拆成两个 watch：`playlist` 保留 deep（`addToPlaylist` 只 push 不改 index），`[currentIndex, currentTime, isPlaying]` 走浅监听（高频部分） | 高频路径深遍历 O(n) → O(1) |
| D7 | [playlist.js](frontend/src/api/playlist.js) + [memoryCache.js](frontend/src/utils/memoryCache.js) | 首页→歌单详情每次进入都重复拉 `/api/playlists`、`/api/songs`；与"同 key 取消"叠加会产生跨页在途请求 | 新增 `createMemoryCache({ ttl: 60000 })`，`getPlaylistsService/getSongsService` 包 60s TTL 缓存；调用方零改动（已核实全部只读） | 60s 内跨页重复请求 2 次 → 0 次 |
| D8 | [perf.js](frontend/src/utils/perf.js) + [main.js](frontend/src/main.js) | **性能数据无采集闭环**——上一轮的性能数字全靠手工 Lighthouse，无法持续追踪 | 原生 PerformanceObserver 采集 FCP/LCP/CLS/TBT（`buffered: true` 覆盖 JS 启动前条目），pagehide/visibilitychange 时 `sendBeacon`（可选端点）+ localStorage 留存最近 20 条 | 四指标采集闭环，数据可离线回溯、可被面试追问 |

### 方向二：稳定性治理（本轮缺得最彻底）

| # | 位置 | 问题 | 修复 | 量化 |
|---|---|---|---|---|
| A1 | [errorHandler.js](frontend/src/utils/errorHandler.js) + [ErrorBoundary.vue](frontend/src/components/ErrorBoundary.vue) + [App.vue](frontend/src/App.vue#L26-L27) | 异常兜底 **0 层**：渲染错误/JS 错误/未捕获 Promise/路由 chunk 失败全部静默，白屏无反馈 | 5 层捕获体系：`app.config.errorHandler` + `window.onerror` + `unhandledrejection` + `router.onError`（**ChunkLoadError 单次自动重拉防死循环**，sessionStorage 标记）+ `ErrorBoundary.onErrorCaptured`（组件级隔离 + fallback + 重试）；错误 1s 节流去重、写 localStorage `app_errors` 环形队列 50 条 | 异常兜底 0 层 → 5 层；chunk 加载失败从白屏 → 自动恢复 1 次 + 兜底提示 |
| A2 | [request.js](frontend/src/utils/request.js) | 请求层无韧性：弱网下 GET 一次失败即失败；快速搜索/重复导航发出 N 个重复请求 | ①幂等 GET/HEAD 指数退避重试（1s/2s，最多 2 次；仅 `!response` 或 status≥500，**401/4xx/取消不重试**，重试期失败静默）；②同 key 重复请求取消（AbortController，后者胜），响应拦截器**第 0 行 `axios.isCancel` 短路**防误弹 | 弱网 GET 成功概率提升；快速搜索重复请求 N → 1 |
| B3 | [store.js](backend/store.js) + [jsonfs.js](backend/jsonfs.js) + [server.js](backend/server.js) + [middleware/user.js](backend/middleware/user.js) | 可变数据接口（favorites/history/searchHistory/users）每次请求**全量读盘 + 全量重写**，且 search-history 存 3 处重复 try/readFileSync 死代码 | 新增 `createJsonStore`：启动读入内存 → 请求走内存读（读 I/O 归零）、`mutate(fn)` 串行写队列 + 10s 防抖落盘、`flushNowSync()` + `process.on('exit'\|'SIGINT'\|'SIGTERM')` 崩溃兜底；`userExists` 改读内存（**注销即失效语义不变**）；删除 3 处死代码 | 可变数据接口磁盘 I/O：每请求 1 读 + 1 全量重写 → **0 读 + 每 10s 至多 1 落盘** |

### 方向三：用户体验

| # | 位置 | 问题 | 修复 | 量化 |
|---|---|---|---|---|
| C5 | [player.js](frontend/src/stores/modules/player.js) + [useAudio.js](frontend/src/composables/useAudio.js) + [PlayPage.vue](frontend/src/views/play/PlayPage.vue#L57-L60) + [MiniPlayer.vue](frontend/src/components/MiniPlayer.vue#L38) | 弱网切歌**无任何加载反馈**（点了像没点）；音频 error 事件静默 console，用户对"歌挂了"零感知 | 新增 `isBuffering` 状态机：`loadstart/waiting/stalled → true`（isPlaying 守卫，不触碰 playSeq 写路径）、`canplay/playing → false`；播放页唱片中央半透明遮罩 + 旋转 loading、迷你播放器标题旁小 loading；音频 error 升级为 toast（MediaError 区分文案，1.5s 节流防快速切歌连弹） | 弱网切歌从"无反馈"→ 明确加载态；音频失败 toast 反馈率 0 → 100% |
| C6 | [variables.scss](frontend/src/styles/variables.scss) + [index.vue](frontend/src/views/layout/index.vue#L34-L40) + [MiniPlayer.vue](frontend/src/components/MiniPlayer.vue#L68) + [PlayPage.vue](frontend/src/views/play/PlayPage.vue) | iPhone 全面屏底部**三处被 Home Indicator 遮挡**：tabbar、迷你播放器、播放页底部控件 | `$safe-area-inset-bottom: max(constant(...), env(...), 0px)`（兼容旧 iOS 的 constant fallback）；tabbar 加 `safe-area-inset-bottom` 属性 + content padding、MiniPlayer bottom 与 PlayPage footer padding 纳入安全区 | 修复 iPhone 全面屏底部 3 处遮挡 |

### 方向四：工程化落地

| # | 位置 | 问题 | 修复 | 量化 |
|---|---|---|---|---|
| B4 | [accessLog.js](backend/accessLog.js) + [server.js](backend/server.js) | 后端**无访问日志**：线上事故无痕、无法统计慢接口/5xx 分布 | 新增基于 `res.on('finish')` 的零依赖中间件，输出 `ISO时间 方法 路径 状态码 耗时ms` | 每请求 1 行结构化日志，可统计慢接口 Top N 与 5xx 分布 |
| E9 | [frontend/.env.example](frontend/.env.example) + [backend/.env.example](backend/.env.example) | 环境变量散落代码/文档，**无一份"配置即文档"**的清单，新成员/部署者易漏配 | 补齐两份 `.env.example`（VITE_API_BASE_URL、VITE_PERF_ENDPOINT / JWT_SECRET、CORS_ORIGINS），注明默认值与用途 | 配置项文档化，部署漏配概率下降 |

> 说明：A2、B3 同时横跨"稳定性"与"工程化"两个方向（请求层契约 / 后端存储工程化），表格归入主受益方向。

---

## 二、验证结果（全部通过）

| 验证项 | 命令/方法 | 结果 |
|---|---|---|
| 代码格式 | `cd frontend && pnpm format` | ✅ 修复 6 个文件 prettier 换行（App.vue / request.js / errorHandler.js / perf.js / index.vue / MiniPlayer.vue） |
| Lint | `cd frontend && pnpm lint` | ✅ 通过（仅 node 版本 WARN，非错误） |
| Build | `cd frontend && pnpm build` | ✅ 成功，545ms；主入口 `index` 18.28KB（gzip 7.16KB），路由分片 0.87~11.38KB |
| 后端冒烟 | `cd backend && node server.js` | ✅ 启动正常，访问日志逐行输出（含 `/api/songs?category=rock` 200） |
| 内存缓存落盘 | 连续 POST favorites 3 次，观察 `favorites.json` mtime | ✅ 只在 10s 防抖 flush 时变化 1 次，期间 3 次写全部进内存 |
| 注销即时失效 | 注册→收藏→注销→旧 token 再调收藏 | ✅ 返回 401（内存 `userExists` 即时失效） |
| dev server 编译 | `cd frontend && pnpm dev`，逐个 curl 新模块 | ✅ 全部 HTTP 200、无编译错误（errorHandler / perf / memoryCache / request / ErrorBoundary / App / api/playlist） |
| 请求取消无误弹 | dev slow 3G 快速搜索 | ✅ Network 只保留最后 1 个请求，无"网络连接失败"误弹（`axios.isCancel` 短路） |
| buffering 状态 | slow 3G 播放 / 断网播放 | ✅ 播放页出现 spinner；断网弹"音频加载失败"（1.5s 节流一次） |
| 列表 TTL 缓存 | 首页→详情→返回→再进 | ✅ 60s 内 `/api/songs` 只发 1 次 |
| 错误体系 | 手动 throw / 断 chunk | ✅ `localStorage.app_errors` 落条；chunk 失败自动重拉 1 次 |
| watch 拆分 | 播放中切歌/拖进度 | ✅ 2s 后 `playerState_*` 正常更新（浅监听无丢失） |
| 安全区 | DevTools iPhone 模拟 | ✅ 底部 3 处无遮挡 |
| perf.js | dev console | ✅ 出现 `fcp/lcp/cls/tbt` console.table |

---

## 三、简历联动（已写入《简历-音乐播放器-第三轮.md》）

1. **第三部分亮点全部来自本轮"可自证"能力**：
   - 前端稳定性治理体系（5 层错误捕获 + chunk 自动恢复 + 本地错误回溯）
   - 后端内存缓存 + 写队列落盘（磁盘 I/O 归零 + 10s 防抖 + 崩溃兜底）
   - 请求层韧性（幂等 GET 指数退避重试 + 同 key 取消）
   - 性能监控闭环（原生 PerformanceObserver 采集 FCP/LCP/CLS/TBT）
   - 列表 TTL 缓存（跨页重复请求 2→0）
2. **面试官评估**：每条亮点配追问 + 安全措辞（崩溃窗口、测量口径、为什么不用 Sentry 等），见简历文件第三节。

---

## 四、遗留事项（明确排除，简历勿写）

| 项 | 原因 |
|---|---|
| 虚拟滚动（列表量级小） | 歌单/歌曲数据量小，收益近零 |
| 深色模式 | 纯 UI 锦上添花，无业务价值，写进简历属"空壳" |
| commitlint / husky 强约束 | 个人项目无多人协作冲突，收益低 |
| CI/CD 自动化 | 无部署目标平台，暂无价值 |
| 自动化测试（单元/E2E） | 时间成本高，本项目以手测覆盖主流程 |
| gzip/brotli 预压缩 | 部署层（Nginx/托管平台）一般自带，前端预压收益低 |
| preload/preconnect / CDN 外部化 | 同源资源收益低，低风险优先 |
| route-level prefetch / 音频预加载下一首 | 移动端流量敏感，预取策略需权衡，暂缓 |

> ⚠️ 简历红线：以上"未落地"项一律不要写进简历——面试官一旦追问实现细节即穿帮，属于"过度包装"风险项。

---

## 五、文件变更清单

### 后端（第 1 批）
| 文件 | 变更类型 |
|---|---|
| backend/jsonfs.js | **新建**：原子写 `writeFileAtomic` / `readJSON` / `writeJSON`，从 server.js 迁移（行为零变化） |
| backend/store.js | **新建**：`createJsonStore`（内存读 + 串行写队列 + 10s 防抖 + `flushNowSync`），导出 users/favorites/history/searchHistory 四 store |
| backend/accessLog.js | **新建**：访问日志中间件（`res.on('finish')`） |
| backend/server.js | 重构：store 化可变数据接口 + 退出 flush + 挂载 accessLog |
| backend/middleware/user.js | 重构：`userExists` 改读内存 store |
| backend/.env.example | **新建**：JWT_SECRET / CORS_ORIGINS 配置清单 |

### 前端（第 2-4 批）
| 文件 | 变更类型 |
|---|---|
| frontend/src/utils/request.js | 重写：幂等 GET 指数退避重试 + 同 key 取消（AbortController）+ `axios.isCancel` 短路 |
| frontend/src/utils/memoryCache.js | **新建**：`createMemoryCache`（Map + TTL 过期） |
| frontend/src/api/playlist.js | 重写：列表接口包 60s TTL 缓存 |
| frontend/src/utils/perf.js | **新建**：PerformanceObserver 四指标采集 + sendBeacon + localStorage 回溯 |
| frontend/src/utils/errorHandler.js | **新建**：5 层错误捕获 + chunk 单次自动恢复 + 1s 节流去重环形队列 |
| frontend/src/components/ErrorBoundary.vue | **新建**：组件级错误隔离 + fallback + 重试 |
| frontend/src/composables/useAudio.js | 编辑：buffering 事件状态机 + 音频 error toast（1.5s 节流） |
| frontend/src/stores/modules/player.js | 编辑：新增 `isBuffering` ref + `setBuffering` action |
| frontend/src/App.vue | 编辑：E10 watch 拆分 + ErrorBoundary 包裹 router-view |
| frontend/src/main.js | 编辑：挂载全局错误体系 + perf 监控 |
| frontend/src/views/play/PlayPage.vue | 编辑：缓冲遮罩 + footer 安全区 |
| frontend/src/components/MiniPlayer.vue | 编辑：标题缓冲 loading + 安全区 |
| frontend/src/views/layout/index.vue | 编辑：tabbar 安全区 + content padding |
| frontend/src/styles/variables.scss | 编辑：`$safe-area-inset-bottom` 变量 |
| frontend/.env.example | **新建**：VITE_API_BASE_URL / VITE_PERF_ENDPOINT 配置清单 |
