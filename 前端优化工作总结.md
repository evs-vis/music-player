# 前端优化工作总结（2026-08-11 第二轮）

> 本轮目标：**先把项目还能优化的真实代码优化掉，再把成果写进简历**。
> 范围（与用户确认）：仅前端 bug 修复 + 代码整洁，不含后端改动。

---

## 一、修复清单（全部代码核实，全部已修复）

### A. 功能 bug（用户可感知）

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| A1 | [SearchPage.vue](frontend/src/views/layout/SearchPage.vue) | 分类卡片传**中文分类名**（含错别字"流行流行"），歌曲 `category` 是英文 id → 点击分类详情页**匹配不到、空列表** | categories 改为英文 id 结构（pop/rock/classical/jazz），跳转传 `cat.id`，与 PlaylistPage 对齐；修掉错别字 |
| A2 | [request.js](frontend/src/utils/request.js) | toast 文案是字面量 `'err.message \|\| ...'`，`\|\|` 被当文本，**永远不显示真实错误** | 改为 `err?.response?.data?.error \|\| 通用文案`，优先透出后端业务错误 |
| A3 | [AppHeader.vue](frontend/src/components/AppHeader.vue) | 未登录 push `/login` 后**缺 return**，又 push `/mine` → 跳转竞态 | 加 return，登录/个人中心二选一 |
| A4 | [MinePage.vue](frontend/src/views/layout/MinePage.vue) | 上传失败 toast 含**乱码字符 `|}`**，且不展示真实错误 | 改为真实错误 + 干净文案 |

### B. 资源释放（规范闭环）

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| B1 | [SearchPage.vue](frontend/src/views/layout/SearchPage.vue) | `useDebounceFn` 无 `onUnmounted` 清理（实测 VueUse 14.4.0 无 scope dispose） | 卸载时 `debouncedSearch.cancel()`，避免切页后 300ms 内仍发无意义请求 |

### C. 死代码清理（整洁）

| # | 位置 | 内容 |
|---|---|---|
| C1 | [SearchPage.vue](frontend/src/views/layout/SearchPage.vue) | 删除约 60 行注释掉的旧 `setTimeout` 防抖实现（含调试 `console.log`）、旧 `doSearch`、`favoritesStore` 未用 import、误导性注释 |
| C2 | [variables.scss](frontend/src/styles/variables.scss) | 删除注释残留的旧主题色 `// $primary-color: #27AE60;` |

---

## 二、验证结果（全部通过）

| 验证项 | 命令 | 结果 |
|---|---|---|
| Lint | `cd frontend && pnpm lint` | ✅ 通过（仅 node 版本 WARN，非错误） |
| Build | `cd frontend && pnpm build` | ✅ 成功，799ms，主入口 JS 14.17KB（gzip 5.54KB） |
| 分类过滤 | `GET /api/songs?category=pop` | ✅ pop 3 首（笨、呼吸决定、为你只为你） |
| 列表剥离歌词 | `GET /api/songs` | ✅ 无 lyrics 字段、有 coverThumb |
| preview proxy 链路 | `pnpm preview` + curl | ✅ rock 分类正确返回 1 首 |
| 残留检查 | grep `goCategory\|cat.name\|流行流行` | ✅ 无残留中文分类名/错别字 |

---

## 三、简历联动（已写入《简历-音乐播放器-优化版.md》）

1. **新增核心贡献第 0 条**："交互链路 bug 修复与错误处理规范化"——涵盖分类跳转、错误文案、跳转竞态、防抖清理、死代码清理。
2. **面试官评估新增追问 8-10 条**：
   - 分类 bug 根因（中文名 vs 英文 category）及"为什么用英文 id 做路由参数"
   - 错误处理契约（后端统一 `{ error }` 结构）
   - `useDebounceFn` 为什么不自动清理（实测无 scope dispose）
3. **修改建议补充 2 条**：把 bug 修复讲成设计原则（英文 id 参数稳定性）；错误契约意识。
4. **事实速查表补充**：分类导航、错误处理 2 行。

---

## 四、遗留（未做，明确排除）

- **后端内存缓存层**（favorites/history/searchHistory 每次请求全量读盘，清单 #32 未修复部分）——用户本轮未选择，留作后续。
- **index.html preload/preconnect**——同源资源收益低，按"低风险为先"跳过。
- 简历顶部在线演示地址占位符、部署时 `JWT_SECRET` 环境变量——仍需用户部署时落实。

---

## 五、文件变更清单

| 文件 | 变更类型 |
|---|---|
| frontend/src/views/layout/SearchPage.vue | 分类 bug + 防抖清理 + 死代码清理 |
| frontend/src/utils/request.js | 错误文案 |
| frontend/src/components/AppHeader.vue | 跳转竞态 |
| frontend/src/views/layout/MinePage.vue | toast 乱码 |
| frontend/src/styles/variables.scss | 注释残留 |
| 简历-音乐播放器-优化版.md | 新增贡献 0 条、追问 8-10、修改建议、速查表 |
