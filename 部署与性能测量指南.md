# 部署与性能测量指南（简历数据来源）

> 目标：拿到能写进简历、且面试时**可复现**的真实性能数据。
> 现状判断：本项目的音频/封面/歌词静态资源托管在 Node 后端，若只要"测首屏性能分"，只部署前端即可；若要让作品**可听**，需一并部署后端（进阶方案见文末）。

---

## 一、当前方案：只部署前端 → PageSpeed 测线上性能

### 为什么够用
PageSpeed Insights 测的是**首屏渲染性能**（HTML/CSS/JS 的加载与绘制），首页的静态结构、Vant 组件、样式、Logo 都会正常渲染，不依赖后端接口成功返回。即使 `/api/songs` 请求失败，**FCP/LCP/性能分仍测的是真实的前端资源加载**。

---

## 二、部署前端到 Vercel（15 分钟）

### 方式 A：Vercel CLI（不用 GitHub，最快）

```bash
# 1. 进入前端目录
cd "b:\vue scaffolding\music-player\frontend"

# 2. 登录 Vercel（浏览器会弹出授权）
npx vercel login

# 3. 首次部署（交互式，按提示选）
npx vercel
```

交互提示怎么选：
- **Framework Preset**：选 `Vite`
- **Build Command**：留空（自动用 `pnpm build`）
- **Output Directory**：`dist`
- **Install Command**：留空（Vercel 检测到 pnpm-lock.yaml 会自动用 pnpm）

部署完成后会输出一个网址，形如 `https://your-project.vercel.app`。

### 方式 B：GitHub + Vercel（推荐，作品能进 GitHub 展示）

1. 先把 frontend 推上 GitHub：
```bash
cd "b:\vue scaffolding\music-player\frontend"
git init
git add -A
git commit -m "feat: music player frontend"
git remote add origin <你的仓库地址>
git push -u origin main
```
2. 打开 [vercel.com](https://vercel.com) → Import → 选刚推的仓库
3. Vercel 自动识别 Vite + pnpm，无需额外配置，点 Deploy 即可

> 注意：根目录的 `.gitignore` 已配好（忽略 backend/data、avatars、node_modules），但 frontend 是独立仓库，提交前确认 frontend/.gitignore 覆盖 `node_modules`、`dist`。

---

## 三、PageSpeed Insights 测量（5 分钟）

1. 打开 **https://pagespeed.web.dev**
2. 输入刚部署的 Vercel 地址
3. 设备选 **Mobile**（移动端 H5 项目用移动端指标）
4. 点 Analyze，约 30 秒出结果
5. **记录并截图**：
   - **Performance 性能分**（0-100）
   - **FCP**（First Contentful Paint，首屏绘制）
   - **LCP**（Largest Contentful Paint，最大内容绘制）
   - **CLS**（布局偏移，越小越好）
   - **TBT**（总阻塞时间）
   - 右上角可切 **Desktop** 再看一组（可选）

---

## 四、本地备选：Lighthouse 快速预看（可选）

如果不想等部署，可本地先看趋势（数字会偏乐观，因为无网络开销，仅供参考排查）：

```bash
# 1. 起后端 + 前端
cd "b:\vue scaffolding\music-player\backend" && node server.js &
cd "b:\vue scaffolding\music-player\frontend" && pnpm dev &

# 2. 跑 Lighthouse（需 Chrome）
npx lighthouse http://localhost:5173 \
  --quiet --chrome-flags="--headless" \
  --only-categories=performance \
  --output=json --output-path=./lh-report.json
```

---

## 五、数据怎么填进简历（口径很重要）

| 数据 | 写法示例 | 面试口径 |
|---|---|---|
| 主 CSS 体积 | `主入口 CSS 由 194KB 降至 59KB（gzip 后约 32KB）` | "构建产物 dist/assets/index-*.css，未 gzip 60638B / gzip 32605B" |
| 主 JS 体积 | `主入口 JS gzip 后约 68KB` | "dist/assets/index-*.js，gzip 69487B" |
| Lighthouse 性能分 | `Lighthouse 移动端性能分 9X` | "线上部署后 PageSpeed Insights Mobile 测的，地址可复现" |
| FCP/LCP | `FCP <1s / LCP <2s` | 同上，记录具体数值 |
| 首屏体积描述 | `路由懒加载 + Vant 按需引入，首屏仅加载主入口与当前路由分块` | 可解释分包策略 |

**面试金句**：数字前面一定带一句 **"线上部署后用 PageSpeed Insights 移动端测的"**——这比数字本身更值钱，表示你知道测量口径。

---

## 六、进阶：让作品可听（后续想做再说）

若想让面试官点开就能完整听歌、登录、收藏，需一并部署后端：

1. **后端部署**：推 GitHub 后用 [Render](https://render.com) 或 [Railway](https://railway.app) 免费层部署，设置环境变量：
   - `JWT_SECRET`（必填，随机字符串）
   - `CORS_ORIGINS=https://your-frontend.vercel.app`
   - 端口用 `process.env.PORT || 3000`（当前 server.js 写死 3000，需小改）
2. **前端指到线上后端**：在 Vercel 项目 → Settings → Environment Variables 加 `VITE_API_BASE_URL=https://your-backend.onrender.com`，重新部署
3. **音频/封面静态资源**：由后端托管，前端通过 `VITE_API_BASE_URL` 拼出 `/audio/...`、`/covers/...`，已支持（useAudio.js / AppHeader / MinePage 都用了 `VITE_API_BASE_URL`）

> ⚠️ 注意：免费层后端会**睡眠**（几分钟无请求就停，冷启动 30s+），面试现场打开可能转圈。可提前预热，或说明"个人项目免费部署，首访需等冷启动"。
