# 简历-音乐播放器-第三轮

> 生成日期：2026-08-11 · 基于第三轮全链路优化后的真实代码
> 护栏：**全文数字均可从代码/构建/冒烟验证复现，无一处虚构**；带【待补充】标记的需用户部署时确认。

---

### 项目名称：移动端 H5 音乐播放器（前后端全栈）
**项目时间**：2025.XX - 2026.08【待补充：请填起始月份】
**担任角色**：独立开发（前端为主，含轻量后端）
**项目简介**：面向移动端的 H5 音乐播放器，Vue 3 + Vite 前端、Express + JSON 文件存储后端，覆盖注册登录 → 浏览/搜索/收藏 → 播放/歌词同步 → 个人中心完整用户闭环，11 首带歌词歌曲资源。

**核心贡献**：

**【第一部分 · Vue 技术栈落地业务】**
1. **构建 8 页面播放器架构**：以 Vue 3 Composition API + Pinia 5 个业务模块，通过 **Pinia watch 驱动 Audio 原生事件与播放状态双向同步**，实现播放/暂停/上下曲/三种播放模式/歌词逐行滚动；用内部标志位修复 Audio 事件与 store 的无限更新循环，并为 11 首歌中 10 首共 427 行时间戳歌词实现 O(1) 定位的同步方案，切歌/拖进度无抖动。**歌词接口拆分**：列表接口剥离 427 行歌词字段减小首屏 JSON，播放/切歌时按需拉取详情接口补全，带切歌防竞态（只写当前歌曲，快速切歌不串歌）。
2. **新增音频缓冲状态机**：基于 Audio `loadstart/waiting/stalled/canplay` 事件驱动 `isBuffering` 状态（isPlaying 守卫），播放页唱片中央与迷你播放器给出明确加载反馈，弱网切歌不再"点了像没点"。

**【第二部分 · 生态库落地具体功能】**
3. **封装移动端 UI 与网络层**：用 Vant 完成播放页/歌单/弹层等组件开发；用 axios 拦截器实现统一 JWT 鉴权、错误分级提示（**升级为幂等 GET 指数退避重试 1s/2s + 同 key 请求取消**，弱网 GET 成功概率提升）；用 @vueuse `useDebounceFn` 实现 300ms 防抖搜索并保证卸载清理；用 Vite 路由懒加载 + Vant 按需引入 + manualChunks，将 15 个路由分片控制在 0.9~11.4KB，主入口 JS 18.28KB（gzip 7.16KB）。

**【第三部分 · 项目亮点（稳定性 / 性能 / 工程化 / 体验）】**
4. **主导前端稳定性治理**：搭建 5 层全局错误捕获体系（Vue `errorHandler` / `window.onerror` / `unhandledrejection` / 路由 chunk 加载失败自动重拉一次 / 组件级 `ErrorBoundary` 隔离），错误 1s 节流去重写入本地环形队列 50 条——异常兜底从 0 层到 5 层，路由 chunk 失败从白屏变为自动恢复。
5. **设计并实现后端内存缓存 + 写队列落盘**：JSON 文件存储改为启动读入内存、写操作串行队列 + 10s 防抖落盘、进程退出/信号同步 flush 兜底，可变数据接口磁盘 I/O 从"每请求 1 次全量读 + 1 次全量重写"降为 **0 读 + 每 10s 至多 1 次落盘**，并保持 token 注销即时失效语义。
6. **建立性能监控闭环**：用原生 `PerformanceObserver`（零依赖）采集 FCP/LCP/CLS/TBT 四指标，页面隐藏时 `sendBeacon` 上报 + localStorage 留存 20 条可离线回溯；列表接口包 60s TTL 内存缓存，首页→详情跨页重复请求 2 → 0。

**【第四部分 · 技术项目解决业务痛点】**
7. **独立开发后端存储工程化**：Express + JSON 文件存储，落地 JWT(HS256) 鉴权、bcrypt 密码加密、原子写（临时文件 + rename）、头像文件魔数校验、CORS 白名单、token 注销即失效、`JWT_SECRET` 环境变量双轨制，解决单机文件存储的**并发写损坏**与**每次请求全量读盘**两大工程痛点。

**技术栈**：`Vue 3` `Composition API` `Pinia` `Vant 4` `Axios` `Vue Router` `@vueuse` `Vite 8` `Express 5` `JSON 文件存储` `JWT` `bcrypt`

---

### 🆕 面试官视角评估

**面试官可能会追问的点**：

1. **（针对第 5 条内存缓存）** "10s 防抖期间进程崩溃会丢多久数据？怎么兜底？" → 这是本项目最可能被深挖的架构点，必须能讲清崩溃窗口与 flush 策略。
2. **（针对第 4 条 5 层错误体系）** "为什么不用 Sentry？"、"`onErrorCaptured` 和 `errorHandler` 的分工是什么？"、"chunk 自动重拉的防死循环机制？"
3. **（针对第 3 条请求重试）** "重试怎么保证幂等？万一重试了 POST 怎么办？" → 必须主动说明"仅 GET/HEAD 且仅 `!response` 或 5xx 重试，401/4xx/取消不重试"。
4. **（针对第 3 条请求取消）** "取消的请求不会误弹 toast 吗？快速搜索时旧请求还在怎么办？" → 要讲 `axios.isCancel` 响应拦截器第 0 行短路。
5. **（针对第 6 条性能监控）** "FCP/LCP/CLS/TBT 分别怎么采的？CLS 为什么用累计？数据怎么自证？" → 需能说出 PerformanceObserver + buffered 口径。
6. **（针对第 1 条 watch 驱动）** "为什么用 Pinia watch 而不是直接在 Audio 事件里改状态？打破循环的内部标志位怎么设计？"
7. **（针对第 1 条歌词接口拆分）** "歌词为什么要单独拆接口？播放页拉歌词的时机和防竞态怎么做的？" → 要讲清三件事：① 列表接口剥离 lyrics 减小首屏 JSON（427 行歌词约 10-20KB 纯文本，列表页用不到）；② `setCurrentSong` 检测缺 lyrics 才请求，切歌/恢复播放都能覆盖；③ 响应回来校验 `currentSong.id` 仍是同一首才写入，防止快速切歌旧歌词覆盖新歌。
8. **（针对第 7 条原子写）** "临时文件 + rename 在 Windows/跨盘符会失败吗？fsync 做了吗？" → 本项目未做 fsync，需准备话术（单机个人项目量级，rename 原子性已覆盖崩溃场景）。

**修改建议**：

- **内存缓存崩溃窗口**：改成安全措辞——"崩溃最多丢失一次 10s 窗口内的增量数据（下次 flush 落盘），已通过进程退出信号同步 flush 收窄窗口；该方案面向单机低并发量级"——**不要**写"零丢失"。
- **第 6 条监控数据自证**：面试前请重新 build 并在本地跑一次，记下四个指标的实测值（口径：本地 Lighthouse/PerformanceObserver，非线上）；`console.table` 可直接在 devtools 展示。
- **第 3 条"弱网 GET 成功概率提升"**：这是逻辑推演不是实测数字，已用"提升"而非具体百分比表述，面试时若要量化，改为讲**重试次数与退避策略**而非虚构的成功率。
- **第 7 条 fsync**：若面试官追问"掉电会不会丢"，坦承"未做 fsync，个人项目量级，生产级应加"，比硬扛更稳。
- **占位符提醒**：项目时间起始月、在线演示地址、GitHub 地址、部署时的 `JWT_SECRET` 请务必补齐后再投递——这是目前最可能的"过度包装"穿帮点。

---

## 附：面试可复现证据表（口径：本地构建/冒烟）

| 简历数字 | 证据位置 | 复现命令 |
|---|---|---|
| 主入口 JS 18.28KB / gzip 7.16KB、路由分片 0.9~11.4KB | `frontend/dist/assets/` | `cd frontend && pnpm build` |
| 10 首带歌词 / 427 行 | `backend/data/songs.json` | `node -e` 读取计数 |
| 歌词接口拆分：列表剥离、播放页按需补全 | [player.js](frontend/src/stores/modules/player.js) `fetchLyrics` + [server.js](backend/server.js) `toListSong` | 首页点歌进播放页，Network 看到 `/api/songs/:id` 详情请求；`curl /api/songs` 无 lyrics、`curl /api/songs/s1` 有 |
| 3 种播放模式 / 切歌无抖动 | [player.js](frontend/src/stores/modules/player.js) playSeq 竞态修复 | 手动切歌验证 |
| 内存缓存 10s 防抖 / 0 读 + 每 10s ≤1 落盘 | [store.js](backend/store.js) | 连续 POST 观察 `favorites.json` mtime |
| 注销即时失效 | [middleware/user.js](backend/middleware/user.js) 读内存 store | 注册→收藏→注销→旧 token 调接口 401 |
| 5 层错误体系 + 50 条环形队列 | [errorHandler.js](frontend/src/utils/errorHandler.js) | 手动 throw，查 `localStorage.app_errors` |
| 幂等 GET 重试 1s/2s、同 key 取消 | [request.js](frontend/src/utils/request.js) | slow 3G 快速搜索，Network 仅留 1 个请求 |
| 60s TTL 列表缓存 2→0 | [memoryCache.js](frontend/src/utils/memoryCache.js) + [playlist.js](frontend/src/api/playlist.js) | 首页→详情→返回→再进，`/api/songs` 只发 1 次 |
| 四指标监控 | [perf.js](frontend/src/utils/perf.js) | dev console 见 `fcp/lcp/cls/tbt` |
| iOS 安全区 3 处 | [variables.scss](frontend/src/styles/variables.scss) | DevTools iPhone 模拟检查底部 |
