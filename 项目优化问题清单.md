# 音乐播放器项目优化问题清单

> 生成日期：2026-08-03 · 由三路并行代码审查（前端核心逻辑 / 后端安全 / 工程化性能）产出，问题均已逐行核实。
> 优先级：🔴 高（必须修） / 🟡 中（应修） / 🟢 低（可优化）
> 文件行号为审查时参考，后续改动后可能偏移。

---

## 一、安全问题（🔴 最优先）

### 1.【🔴 高】JWT 密钥硬编码，可伪造任意用户 token
- **位置**：`backend/middleware/user.js:2`、`backend/server.js:11`（两处重复定义）
- **现状**：`SECRET = 'music-player-secret-key'` 是公开可猜测的固定字符串，`server.js:140` 用它签名、`user.js:11` 用它校验。
- **改的原因**：拿到源码的人可自签 `{userId: 任意id}` 的合法 token，冒充任意用户访问收藏/历史/改密/上传头像/播放状态等所有受保护接口，甚至覆盖他人数据。**本项目最严重漏洞。**
- **改法**：`SECRET` 改为环境变量 `process.env.JWT_SECRET`，两个文件统一从一个模块导出；`jwt.verify` 显式 `{ algorithms: ['HS256'] }`。

### 2.【🔴 高】JSON 文件非原子写入 + 读改写竞态，数据丢失/损坏
- **位置**：`backend/server.js:21-30`（`readJSON`/`writeJSON` 全 `readFileSync`/`writeFileSync`），写调用点遍布所有接口（注册/收藏/历史/搜索历史/改密/头像/播放器）
- **现状**：`writeFileSync` 直接覆写目标文件，非原子——写入中进程崩溃会留半截 JSON；多个请求并发"读-改-写"同一文件，后写覆盖前写（丢更新）。如两个用户同时上传头像、并发收藏/历史。
- **改的原因**：数据丢失、JSON 损坏导致接口 500；多用户操作时高概率触发。**本项目后端最核心的可靠性缺陷。**
- **改法**：写临时文件 + `fs.renameSync` 原子替换；每个文件加写队列/互斥锁串行化；损坏时 try/catch 恢复。

### 3.【🔴 高】`readJSON` 无错误处理，文件损坏即进程崩溃
- **位置**：`backend/server.js:21-25`
- **现状**：`readFileSync` + `JSON.parse` 无 try/catch，且在同步的 Express 路由处理器中执行——同步抛错会冒泡到进程，**整个 Node 进程崩溃**。users/favorites/history/playerState/songs 全部裸调用。
- **改的原因**：任一数据文件损坏（可由问题 2 并发写触发）即服务宕机。
- **改法**：`readJSON` 内 try/catch 返回 `null`，由统一错误中间件处理。

### 4.【🟡 中】CORS 全放行，任意站点可跨域调用接口
- **位置**：`backend/server.js:16` `app.use(cors())`
- **现状**：默认 `Access-Control-Allow-Origin: *`，不限制方法/头。
- **改的原因**：任意恶意网页可在受害者浏览器上下文调用本后端接口（配合 token 窃取/前端 XSS 可直接操作数据）。
- **改法**：`cors({ origin: ['http://localhost:5173'], methods: [...], credentials: false })`。

### 5.【🟡 中】上传头像只校验扩展名，未校验 MIME/魔数
- **位置**：`backend/server.js:346-354`
- **现状**：`fileFilter` 只查 `path.extname(file.originalname)` 白名单，不查 `mimetype`/文件头；`express.static` 直接托管 `public/`。
- **改的原因**：可上传"改名为 .jpg 的 HTML/脚本"，在部分浏览器以可执行内容打开 → **存储型 XSS**。
- **改法**：加 `mimetype` 白名单 + 校验文件头魔数（file-type）；`filename` 不用用户可控扩展名，由服务端定 `.jpg`。

### 6.【🟡 中】注册接口无密码强度校验，弱口令账户
- **位置**：`backend/server.js:92-110`
- **现状**：只检查 username/password 非空，可注册单字符用户名、空白密码。
- **改的原因**：弱密码账户易被爆破；异常用户名造成数据脏。
- **改法**：用户名长度 3-20 + 正则 `^[\w一-龥-]+$`；密码至少 6 位含字母数字。

### 7.【🟢 低】JWT 校验未显式限制签名算法
- **位置**：`backend/middleware/user.js:11`
- **现状**：当前 `jsonwebtoken@9` 默认拒 `alg:none`、只允许 HS 系列，是"侥幸正确"。
- **改的原因**：未来若换密钥类型/RSA 公钥，未显式声明可能产生算法混淆。
- **改法**：`jwt.verify(token, SECRET, { algorithms: ['HS256'] })`。

### 8.【🟢 低】用户数据随源码分发，含真实密码哈希
- **位置**：`backend/data/users.json`
- **现状**：5 个真实账号 bcrypt 哈希在源码目录，会随仓库/备份分发；头像路径暴露 `u+时间戳` 的 id 规律。
- **改的原因**：代码库泄露 → 离线爆破短弱密码哈希；隐私残留。
- **改法**：数据移出分发目录 + 加入 `.gitignore`；生产换数据库。

### 9.【🟢 低】注册/登录/改密信任 `req.body` 类型，畸形 JSON 报 500
- **位置**：`backend/server.js:93,125,303`
- **现状**：`req.body` 传 `null`/数组时 `const { username } = req.body` 抛 TypeError。
- **改法**：入口校验 `req.body` 为普通对象否则 400。

---

## 二、功能 bug（🔴 前端最优先）

### 10.【🔴 高】进度条拖动回弹
- **位置**：`PlayPage.vue:80-85` + `useAudio.js:20-26`
- **现状**：拖动 `touchmove` 写 `currentTime`，同时 Audio `timeupdate` 又把它写回旧值。
- **改的原因**：拖进度条时播放头来回跳，体验差。
- **改法**：拖动开始设 `dragging` 标记；拖动中只写 `seekTime`、禁止 `timeupdate` 写回；`touchend/mouseup` 才真正 seek。

### 11.【🔴 高】快速切歌竞态，可能播错歌
- **位置**：`useAudio.js:67-96`
- **现状**：`loadAndPlay` 是 async，`await el.play()` 挂起期间再切歌，旧 promise 恢复后可能覆盖新歌。
- **改的原因**：连点下一首/上一首，播的不是你点的，状态与音频不一致。
- **改法**：引入单调递增播放序号 `playSeq`，`await play()` 后校验是否最新，否则忽略/暂停。

### 12.【🔴 高】`isPlaying` 双写语义混乱
- **位置**：`player.js:89-95` + `useAudio.js:48-55`
- **现状**：store 的 `isPlaying` 是用户意图标记，`pause/play` 事件又反向改写它；`togglePlay` 只翻转布尔从不调音频。
- **改的原因**：暂停/自动切歌后图标与实际播放不一致（尤其单曲循环、ended 自动切歌后）。
- **改法**：`isPlaying` 只由音频事件驱动（真实状态），另加 `playIntent` 表示意图；store 方法一一定位到音频操作。

### 13.【🔴 高】分类路由传中文 name，详情页匹配不到 → 空列表
- **位置**：`PlaylistPage.vue:43-45` + `PlaylistDetailPage.vue:51` + `SearchPage.vue:130-132`
- **现状**：`goCategory` 传 `cat.name`（中文如"流行"），详情页先按 `id` 匹配再按 `s.category === param`，中文参数大概率匹配不到。
- **改的原因**：歌单分类页点分类进详情是空的。
- **改法**：统一传 `cat.id`（英文），详情页按 id 匹配。

### 14.【🔴 高】歌单详情页收藏传整个 song 对象而非 id
- **位置**：`PlaylistDetailPage.vue:102` + `favorites.js:19`
- **现状**：`toggleFavorite(songId)` 里实际传整个 song 对象，后端收到对象而非 id。
- **改的原因**：歌单详情页收藏功能失效（PlayPage 传 `id` 是对的，两处不一致）。
- **改法**：`toggleFavorite(song.id)`。

### 15.【🟡 中】切歌/删歌后 `currentTime` 未重置，进度条错乱
- **位置**：`player.js:41-47,157-170` + `useAudio.js:28-30`
- **现状**：`setPlaylist`/`playSong`/`removeFromPlaylist` 都不重置 `currentTime`；`loadedmetadata` 更新 `duration` 有延迟。
- **改的原因**：新歌进度条从旧时间点起跳，可能 >100% 或显示错误。
- **改法**：切换 currentSong 时同步重置 `currentTime=0`，play 前 `el.currentTime=0`。

### 16.【🟡 中】`volume` 从 localStorage 读到 NaN 无兜底
- **位置**：`player.js:15`
- **现状**：`parseFloat(localStorage.getItem('volume') || '1')`，垃圾值返回 NaN → `audio.volume = NaN` 抛错。
- **改的原因**：手动改 localStorage 或 persist 存入非法值即异常。
- **改法**：`Number.isFinite(v) ? Math.max(0, Math.min(1, v)) : 1`。

### 17.【🟡 中】`currentIndex=-1` 时 playNext/playPrev 跳错歌
- **位置**：`player.js:97-123`
- **现状**：playlist 非空但 currentIndex=-1（如恢复后非法）时，next 跳到第 0 首、prev 跳到倒数第二首。
- **改法**：playNext/playPrev 前校验 `currentIndex >= 0`，否则先置 0。

### 18.【🟡 中】登录恢复播放不恢复进度
- **位置**：`auth.js:59-61` 等
- **现状**：登录恢复列表但 `duration` 不恢复、seek 时间点不自动执行；`resumeOnGesture` 只恢复"播放"不恢复"进度"。
- **改的原因**：重新登录后不续播到上次位置，体验断档。
- **改法**：恢复后通过 `seekTime` 触发一次 seek（在 resumeOnGesture 点击 handler 里带上）。

### 19.【🟢 低】`removeFromPlaylist`/`clearPlaylist` 后 Audio 实例状态不一致
- **位置**：`PlaylistSheet.vue:42-45` + `player.js:172-176`
- **现状**：清空后 `currentSongKey` 未清空，若再播同一首歌可能复用旧缓冲。
- **改法**：清空时同步清 `currentSongKey`/调用 `pause()`。

---

## 三、资源释放 / 边界（🟡-🟢）

### 20.【🟢 低】歌词 rAF 动画卸载时未取消
- **位置**：`PlayPage.vue:178-227`
- **现状**：`scrollAnimFrames` WeakMap 记录的动画帧无 `onUnmounted` 取消。
- **改法**：`onUnmounted` 里 cancelAnimationFrame。

### 21.【🟢 低】SearchPage 防抖定时器卸载后仍执行
- **位置**：`SearchPage.vue:55-74`
- **现状**：无 `onUnmounted` 清理 timer，切页后 300ms 内仍发一次无意义请求。
- **改法**：`onUnmounted(() => clearTimeout(timer))`。

### 22.【🟢 低】歌词每次 timeupdate 全量线性扫描
- **位置**：`PlayPage.vue:69-77`
- **现状**：`currentLyricIndex` 对全部歌词行做尾部到头的线性扫描（歌词几百行时）。
- **改法**：歌词按时间排序，用二分查找或"上一索引+1 往后探测"增量算法。

---

## 四、工程化 / 性能

### 23.【🔴 高】Vant 全量 CSS 被全量打包
- **位置**：`main.js:8` `import 'vant/lib/index.css'`
- **现状**：全量 CSS 199KB，产物 `index-*.css` 达 194KB（含 1362 个 `van-*` 类，大量组件未使用）。
- **改的原因**：首屏最大请求之一，移动端影响 LCP/FCP；与简历"Vant 按需引入减小体积"名不副实。
- **改法**：`VantResolver({ importStyle: true })` + 删除全量 CSS import，CSS 预计降到 50-70KB。

### 24.【🟡 中】`format` 脚本坏掉
- **位置**：`package.json:13` `prettier --write --experimental-cli`
- **现状**：`--experimental-cli` flag 在 Prettier 3.9.5 已移除，format 脚本报 unknown option。
- **改的原因**：团队无法统一格式化。
- **改法**：删掉该 flag。

### 25.【🟡 中】lint-staged 缺 prettier、lint 脚本带 `--fix`
- **位置**：`package.json:10-12,51-53`
- **现状**：lint-staged 只跑 eslint；`lint:*` 脚本带 `--fix` 会改文件。
- **改的原因**：提交时不格式化；lint 职责混乱。
- **改法**：lint-staged 加 `prettier --write`；lint 脚本去掉 `--fix`。

### 26.【🟡 中】`.gitignore` 缺失（backend 无）
- **位置**：frontend 有 .gitignore，backend/根目录无
- **现状**：`data/users.json`（密码哈希）、`public/avatars/`（用户上传）未被忽略，可能被提交。
- **改的原因**：隐私泄露风险。
- **改法**：根目录建 .gitignore 统一忽略 `**/node_modules`、`**/dist`、`.env`、`backend/data/`、`backend/public/avatars/`。

### 27.【🟡 中】`uploadAvatar` 的 `header` 应为 `headers`
- **位置**：`src/api/auth.js:9-12`
- **现状**：axios 用 `headers`，写错为 `header` 请求头不生效。
- **改的原因**：隐藏 bug，上传请求头设置失效（碰巧 multer 不依赖）。
- **改法**：`header` → `headers`。

### 28.【🟢 低】2.7MB 未使用图片资源
- **位置**：`src/assets/1.png ~ 11.png`（合计约 2.68MB）
- **现状**：全项目无引用，纯残留。
- **改的原因**：污染仓库、影响克隆体积与 IDE 索引。
- **改法**：删除。

### 29.【🟢 低】`playerState.json` 后端接口是死功能
- **位置**：`server.js:48-50,383-406`
- **现状**：前端实际用 localStorage 存播放状态，全项目无任何代码调用后端 player 接口；42KB 文件每次 POST 全量重写。
- **改的原因**：死功能 + 白消耗写开销；两套机制不一致易混乱。
- **改法**：删除接口与文件，或真正接入前端。

### 30.【🟢 低】`volume` 在 persist 与手动 setItem 重复持久化
- **位置**：`player.js:15,142-145,213-217`
- **现状**：`persist.paths` 已含 volume，`setVolume` 又手动 `localStorage.setItem` 双写，且拖动音量时高频写。
- **改法**：删手动 setItem，统一走 persist。

### 31.【🟢 低】死代码/残留：`ElMessage` 全局声明、`header` 拼写、注释残留
- **位置**：`eslint.config.js:20-23`、`main.js:5`、`router/index.js:10,12`、`player.js:125-131,138-140,192`
- **现状**：Element Plus 全局变量（项目用 Vant）从未使用；`variables.scss` import 注释残留；router 两条 `/` 规则；store 多个无调用方别名（`prev/next/setProgress/hasCurrentSong`）。
- **改的原因**：误导维护者（以为在用 Element Plus）。
- **改法**：删除残留。

---

## 五、性能（后端）

### 32.【🟡 中】每次请求全量读取整个 JSON 文件（同步 I/O 阻塞事件循环）
- **位置**：`server.js:21-25`，调用点遍布所有接口
- **现状**：收藏/历史/搜索历史/播放器/用户数据每次请求 `readFileSync` + `JSON.parse`；播放器文件 42KB、songs 46KB 且会增长。
- **改的原因**：多用户高并发时磁盘 IO + CPU 成为瓶颈，同步 I/O 阻塞事件循环拖慢所有接口。
- **改法**：只读数据（songs）启动时缓存一次；可变数据内存缓存 + 定时持久化，或换 SQLite。

### 33.【🟢 低】`/api/songs` 无条件返回全部歌词数据
- **位置**：`server.js:55-73,76-83`
- **现状**：列表接口返回每首歌完整 lyrics（s2 上百条），前端只 `slice(0,10)`。
- **改的原因**：首屏多传不需要的数据；歌词是版权内容时无权限接口泄露完整歌词。
- **改法**：列表接口剔除 `lyrics`，仅详情接口返回。

### 34.【🟢 低】bcrypt 使用同步版本阻塞事件循环
- **位置**：`server.js:102,135,328`（`hashSync`/`compareSync`）
- **现状**：cost=10 的 CPU 密集同步计算在事件循环上运行。
- **改的原因**：高并发登录/注册时每请求阻塞进程数十毫秒。
- **改法**：改用 `bcrypt.hash/compare` 异步版本。

---

## 六、API 设计 / 数据一致性（🟢 低，但有价值）

### 35.【🟢 低】登录/注册失败一律 400，状态码语义错误
- **位置**：`server.js:95,99,132,137,317-323`
- **现状**：认证失败返回 400（应 401）、用户名已存在返回 400（应 409）、参数缺失返回 400（应 422）。
- **改的原因**：契约混乱，前端拦截器只对 401 登出。
- **改法**：区分 401/409/422。

### 36.【🟢 低】修改密码接口请求体嵌套 `req.body.data` 结构脆弱
- **位置**：`server.js:303` + `src/api/auth.js:20-24`
- **现状**：前端 `request.put('/api/user/password', { data })`，后端从 `req.body.data` 解构。
- **改的原因**：契约脆弱，换客户端难复刻。
- **改法**：后端 `req.body` 直接解构，前端直接传 `{ oldPassword, newPassword }`。

### 37.【🟢 低】历史 `sort` 原地修改数组
- **位置**：`server.js:194`
- **现状**：`userHistory.sort(...)` 原地改。
- **改的原因**：未来若写回会造成意外副作用。
- **改法**：`[...userHistory].sort(...)`。

### 38.【🟢 低】注册用 `Date.now()` 生成用户 id，同刻注册会重复
- **位置**：`server.js:104`
- **现状**：毫秒级同刻注册产生重复 id 互相覆盖。
- **改法**：`crypto.randomUUID()`。

### 39.【🟢 低】无注销接口，孤儿数据永久残留
- **位置**：`server.js:112-118` 及 data 文件
- **现状**：每个注册用户在 4 个文件留数据，无法清理。
- **改法**：提供注销接口 + 原子清理关联数据。

### 40.【🟢 低】`playerState` POST 接受任意结构，未校验 songId
- **位置**：`server.js:395-406`
- **现状**：playlist 元素结构由请求体决定，未与 songs.json 交叉校验。
- **改法**：服务端只存 id 列表，前端拉详情。

---

## 七、判断：哪些"值得做"、哪些"对此规模过度"

**必做（性价比高，直接支撑简历与稳定性）**
- #1 JWT 密钥、#2 JSON 原子写、#3 readJSON 兜底 —— 安全+稳定核心
- #4 CORS、#5 上传 MIME —— 安全加固
- #23 Vant 按需 CSS —— 简历"按需引入"名副其实
- #10-12 前端三大播放器 bug —— 用户可感知

**建议做（规范闭环）**
- #24-27 工程化（format/lint-staged/.gitignore/header→headers）
- #15-18 播放器边界（时间重置/NaN/索引/登录恢复）

**个人作品可接受 / 不必投入**
- 虚拟滚动、分页参数、数据库迁移（SQLite）、CI 矩阵、横屏适配
- 当前前端 bundle 810KB、后端单机文件存储，规模够用；等真到瓶颈再上

---

## 附：修复顺序建议

```
第 1 批（安全，1 天）：#1 JWT → #2 原子写 → #3 readJSON 兜底 → #4 CORS → #5 上传 MIME
第 2 批（功能 bug）：#10 进度条 → #11 切歌竞态 → #12 isPlaying → #13 分类路由 → #14 收藏传参 → #15-18 边界
第 3 批（工程化）：#23 Vant 按需 → #24-27 规范 → #16 volume 兜底
第 4 批（打磨）：#20-22 资源释放 → #30 重复持久化 → 其余低优先级
```
